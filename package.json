{
  "name": "openai-image-cli",
  "version": "1.0.0",
  "description": "CLI tool for generating images using OpenAI's DALL-E models",
  "main": "dist/index.js",
  "type": "module",
  "bin": {
    "openai-image": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "prepublishOnly": "npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "openai",
    "dall-e",
    "image-generation",
    "cli",
    "npx"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "openai": "^6.9.0"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "tsx": "^4.20.6",
    "typescript": "^5.9.3"
  }
}
